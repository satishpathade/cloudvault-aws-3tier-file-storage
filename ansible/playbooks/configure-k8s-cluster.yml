---
- name: Configure Kubernetes Master
  hosts: cicd
  become: true

  roles:
    - k8s-master

- name: Configure Kubernetes Workers
  hosts:
    - cloudvault-web-server
    - cloudvault-app-server
  become: true

  roles:
    - k8s-worker